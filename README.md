JavaScript DOM project -- part of The Odin Project: Foundations --
to create grid of square divs that function like an Etch-a-Sketch
by hovering over and activating the divs.

Uses DOM to make grid divs